<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rails RCC Remote" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="app-shell">
      <header class="top-bar">
        <div class="top-bar__lane top-bar__lane--left">
          <div class="brand-wrap">
            <%= link_to root_path, class: "brand" do %>
              <span class="brand-mark" aria-hidden="true">RR</span>
              <span class="brand-text">
                <strong>Rails RCC Remote</strong>
                <small>Automation control plane</small>
              </span>
            <% end %>
          </div>
        </div>

        <nav class="top-bar__nav top-bar__nav--omakase" aria-label="Primary">
          <%= nav_link_to "Dashboard", root_path %>
          <%= nav_link_to "Robots", robots_path %>
          <%= nav_link_to "Catalogs", catalogs_path %>
          <%= nav_link_to "Hololib ZIPs", hololib_zips_path %>
        </nav>

        <div class="top-bar__lane top-bar__lane--right" aria-hidden="true"></div>
      </header>

      <main class="page">
        <% flash.each do |type, message| %>
          <div class="flash <%= type %>"><%= message %></div>
        <% end %>

        <%= yield %>
      </main>
    </div>
  </body>
</html>
