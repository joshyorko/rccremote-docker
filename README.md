# README

Quick links:

- Deployment bootstrap: `DEPLOY_SETUP.md`

Service layout:

- `admin.joshyorko.com` -> Rails admin UI and RCC management actions
- `rccremote.joshyorko.com` -> dedicated `rccremote` daemon endpoint for RCC clients
